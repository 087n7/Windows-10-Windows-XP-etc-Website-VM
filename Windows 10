<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Windows 10 Mockup</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    <style>
        :root {
            --taskbar-height: 48px;
            --taskbar-bg: rgba(31, 31, 31, 0.95);
            --start-menu-width: 320px;
            --start-menu-bg: rgba(34, 34, 34, 0.97);
            --window-bg: #f3f3f3;
            --window-border: #cfcfcf;
            --window-title-bar-bg: #0078d7;
            --text-color-light: #fff;
            --text-color-dark: #333;
            --hover-bg: #444;
            --font-family: 'Segoe UI', Arial, sans-serif;
            --icon-size: 64px;
        }

        body {
            margin: 0;
            background: url('https://wallpapercave.com/wp/wp2552026.jpg') no-repeat center center fixed;
            background-size: cover;
            font-family: var(--font-family);
            height: 100vh;
            overflow: hidden;
            user-select: none;
            cursor: default;
        }

        /* --- Desktop --- */
        .desktop {
            height: calc(100vh - var(--taskbar-height));
            padding: 30px 20px;
            display: flex;
            flex-direction: column;
            flex-wrap: wrap;
            align-content: flex-start;
            gap: 20px;
            position: relative;
            z-index: 1;
        }

        .icon {
            width: 80px;
            text-align: center;
            color: var(--text-color-light);
            cursor: pointer;
            text-shadow: 1px 1px 2px #000;
            transition: background 0.2s;
            border-radius: 4px;
            padding: 5px;
        }

        .icon:hover {
            background: rgba(255, 255, 255, 0.2);
        }

        .icon img {
            width: var(--icon-size);
            margin-bottom: 5px;
            filter: drop-shadow(1px 1px 2px #000);
        }

        /* --- Taskbar --- */
        .taskbar {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            height: 56px; /* Increased height */
            background: var(--taskbar-bg);
            display: flex;
            align-items: center;
            padding: 0 8px;
            box-shadow: 0 -1px 8px rgba(0, 0, 0, 0.5);
            z-index: 100;
        }

        .taskbar-start, .taskbar-item, .taskbar-time, .taskbar-notification {
            cursor: pointer;
            display: flex; /* Added for alignment */
            align-items: center; /* Added for alignment */
            justify-content: center; /* Added for alignment */
            padding: 0 10px; /* Adjusted padding */
            height: 100%; /* Make items fill the taskbar height */
        }
        
        .taskbar-start img {
            width: 36px; /* Slightly larger Start icon */
        }
        
        .taskbar-search {
            margin-left: 10px;
            display: flex;
            align-items: center;
            background: #ffffff; /* Changed to white */
            border-radius: 4px;
            padding: 8px 16px; /* Increased padding */
            min-width: 250px; /* Made search bar wider */
            height: 38px;
        }
        
        .taskbar-search i {
            color: #555; /* Adjusted icon color for contrast on white background */
            margin-right: 10px;
        }
        
        .taskbar-search input {
            background: transparent;
            color: #333; /* Adjusted text color for contrast on white background */
            border: none;
            width: 100%;
            font-size: 16px; /* Slightly larger font */
            outline: none;
        }

        .taskbar-icons {
            display: flex;
            align-items: center;
            margin-left: 10px;
        }

        .taskbar-icons .taskbar-item {
            width: 50px; /* Larger icon containers */
            height: 50px;
            transition: background 0.2s;
            border-bottom: 2px solid transparent;
        }
        
        .taskbar-icons .taskbar-item:hover {
            background: rgba(255, 255, 255, 0.1);
        }
        
        .taskbar-icons .taskbar-item.active {
            border-color: #00a0e2;
        }
        
        .taskbar-icons img {
            width: 32px; /* Slightly larger icons */
        }
        
        .taskbar-right {
            display: flex;
            align-items: center;
            margin-left: auto;
            gap: 12px;
            font-size: 15px; /* Slightly larger font */
            color: var(--text-color-light);
            padding-right: 12px;
        }
        
        .taskbar-notification {
            height: 100%;
            padding: 0 10px;
        }

        .taskbar-notification i {
            font-size: 20px; /* Larger bell icon */
        }
        
        .taskbar-time {
            padding: 0 10px;
            line-height: 1.2;
            text-align: right;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        /* --- Start Menu --- */
        .start-menu {
            position: fixed;
            bottom: calc(var(--taskbar-height) + 8px);
            left: 12px;
            width: 320px;
            height: 560px; /* Made menu taller to resemble Windows 10 */
            background: rgba(45, 45, 45, 0.98); /* Adjusted background for contrast */
            border-radius: 8px;
            box-shadow: 0 2px 16px rgba(0, 0, 0, 0.7);
            color: var(--text-color-light);
            display: none;
            flex-direction: column;
            z-index: 1000;
            animation: fadeIn 0.2s;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .start-menu-header {
            padding: 16px;
            display: flex;
            align-items: center;
            border-bottom: 1px solid #444;
        }

        .start-menu-header img {
            width: 32px;
            margin-right: 12px;
        }
        
        .start-menu-apps {
            padding: 12px 0;
            flex-grow: 1; /* Allows apps section to expand */
            overflow-y: auto; /* Adds scrollbar if needed */
        }
        
        .start-menu-app {
            display: flex;
            align-items: center;
            padding: 8px 18px;
            cursor: pointer;
            transition: background 0.15s;
        }
        
        .start-menu-app:hover {
            background: var(--hover-bg);
        }
        
        .start-menu-app img {
            width: 28px;
            margin-right: 10px;
        }
        
        .start-menu-power {
            padding: 12px 18px;
            display: flex;
            align-items: center;
            cursor: pointer;
            border-top: 1px solid #444;
            transition: background 0.15s;
        }
        
        .start-menu-power:hover {
            background: var(--hover-bg);
        }
        
        .start-menu-power i {
            font-size: 22px;
            margin-right: 8px;
        }

        /* --- Window --- */
        .window {
            position: absolute;
            top: 120px;
            left: 200px;
            width: 340px;
            min-height: 200px;
            background: var(--window-bg);
            border-radius: 6px;
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.2);
            border: 1px solid var(--window-border);
            font-family: var(--font-family);
            z-index: 999;
            display: none; /* Initially hidden */
            flex-direction: column;
            overflow: hidden;
            resize: both;
            min-width: 200px;
            min-height: 150px;
        }
        
        .window.active {
            z-index: 2000;
        }
        
        .window-title-bar {
            background: var(--window-title-bar-bg);
            color: var(--text-color-light);
            padding: 8px 12px;
            border-top-left-radius: 6px;
            border-top-right-radius: 6px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            cursor: move;
            flex-shrink: 0;
        }
        
        .window-title-buttons {
            display: flex;
            gap: 2px;
        }
        
        .window-title-buttons button {
            background: transparent;
            border: none;
            color: var(--text-color-light);
            font-size: 16px;
            cursor: pointer;
            width: 32px;
            height: 24px;
            transition: background 0.2s;
            border-radius: 2px;
        }
        
        .window-title-buttons button:hover {
            background: rgba(255, 255, 255, 0.15);
        }
        
        .window-title-buttons .close-btn:hover {
            background-color: #e81123;
        }

        .window-content {
            padding: 16px;
            color: var(--text-color-dark);
            flex-grow: 1;
            overflow: auto;
        }
    </style>
</head>
<body>
    <div class="desktop" id="desktop">
        <div class="icon" id="documents-icon" data-window-target="#documents-window">
            <img src="https://img.icons8.com/windows/96/000000/folder-invoices.png" alt="Documents">
            <span>Documents</span>
        </div>
        <div class="icon" id="file-explorer-icon" data-window-target="#file-explorer-window">
            <img src="https://img.icons8.com/color/96/000000/file-explorer.png" alt="File Explorer">
            <span>File Explorer</span>
        </div>
    </div>

    <div class="taskbar">
        <div class="taskbar-start" id="start-button">
            <img src="https://img.icons8.com/color/48/000000/windows-10.png" alt="Start">
        </div>
        <div class="taskbar-search">
            <i class="fa-solid fa-magnifying-glass"></i>
            <input type="text" placeholder="Type here to search...">
        </div>
        <div class="taskbar-icons">
            <div class="taskbar-item active" data-window-target="#file-explorer-window">
                <img src="https://img.icons8.com/color/48/000000/file-explorer.png" alt="File Explorer">
            </div>
            <div class="taskbar-item">
                <img src="https://img.icons8.com/color/48/000000/chrome.png" alt="Chrome">
            </div>
            <div class="taskbar-item">
                <img src="https://img.icons8.com/color/48/000000/microsoft-edge.png" alt="Edge">
            </div>
        </div>
        <div class="taskbar-right">
            <div class="taskbar-notification">
                <i class="fa-regular fa-bell"></i>
            </div>
            <div class="taskbar-time" id="taskbarTime"></div>
        </div>
    </div>

    <div class="start-menu" id="start-menu">
        <div class="start-menu-header">
            <img src="https://img.icons8.com/color/48/000000/user.png" alt="User">
            <span>John Doe</span>
        </div>
        <div class="start-menu-apps">
            <div class="start-menu-app" data-window-target="#file-explorer-window">
                <img src="https://img.icons8.com/color/48/000000/file-explorer.png">
                <span>File Explorer</span>
            </div>
            <div class="start-menu-app">
                <img src="https://img.icons8.com/color/48/000000/settings.png">
                <span>Settings</span>
            </div>
            <div class="start-menu-app">
                <img src="https://img.icons8.com/color/48/000000/paint.png">
                <span>Paint</span>
            </div>
        </div>
        <div class="start-menu-power">
            <i class="fa-solid fa-power-off"></i>
            <span>Power</span>
        </div>
    </div>

    <div class="window" id="file-explorer-window">
        <div class="window-title-bar">
            <span>File Explorer</span>
            <div class="window-title-buttons">
                <button class="minimize-btn">_</button>
                <button class="maximize-btn">□</button>
                <button class="close-btn">×</button>
            </div>
        </div>
        <div class="window-content">
            <p>This is a sample window!</p>
        </div>
    </div>

    <div class="window" id="documents-window">
        <div class="window-title-bar">
            <span>Documents</span>
            <div class="window-title-buttons">
                <button class="minimize-btn">_</button>
                <button class="maximize-btn">□</button>
                <button class="close-btn">×</button>
            </div>
        </div>
        <div class="window-content">
            <p>Welcome to your documents!</p>
        </div>
    </div>
    
    <script>
        const desktop = document.getElementById('desktop');
        const startMenu = document.getElementById('start-menu');
        const startButton = document.getElementById('start-button');
        const windows = document.querySelectorAll('.window');
        const taskbarTime = document.getElementById('taskbarTime');

        // Update time on taskbar
        function updateTime() {
            const now = new Date();
            const timeStr = now.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
            const dateStr = now.toLocaleDateString();
            taskbarTime.innerHTML = `<span>${timeStr}</span><span>${dateStr}</span>`;
        }
        updateTime();
        setInterval(updateTime, 1000);

        // Toggle Start Menu
        startButton.addEventListener('click', (e) => {
            e.stopPropagation();
            startMenu.style.display = startMenu.style.display === 'flex' ? 'none' : 'flex';
        });

        // Hide Start Menu on outside click
        document.addEventListener('click', (e) => {
            if (startMenu.style.display === 'flex' && !startMenu.contains(e.target) && !startButton.contains(e.target)) {
                startMenu.style.display = 'none';
            }
        });

        // Window Management
        document.addEventListener('click', (e) => {
            let target = e.target;
            const windowTarget = target.closest('[data-window-target]');
            if (windowTarget) {
                const windowId = windowTarget.dataset.windowTarget;
                const windowEl = document.querySelector(windowId);
                if (windowEl) {
                    windows.forEach(win => win.classList.remove('active'));
                    windowEl.style.display = 'flex';
                    windowEl.classList.add('active');
                    startMenu.style.display = 'none'; // Close start menu when an app is opened
                }
            } else if (target.closest('.close-btn')) {
                const windowEl = target.closest('.window');
                windowEl.style.display = 'none';
            } else if (target.closest('.minimize-btn')) {
                const windowEl = target.closest('.window');
                windowEl.style.display = 'none';
            }
        });
        
        // Window Dragging and Z-index
        let activeWindow = null;
        desktop.addEventListener('mousedown', (e) => {
            const titleBar = e.target.closest('.window-title-bar');
            if (!titleBar) return;
            
            activeWindow = titleBar.closest('.window');
            windows.forEach(win => win.classList.remove('active'));
            activeWindow.classList.add('active');
            
            const offsetX = e.clientX - activeWindow.offsetLeft;
            const offsetY = e.clientY - activeWindow.offsetTop;
            
            function onMouseMove(e) {
                let x = e.clientX - offsetX;
                let y = e.clientY - offsetY;
                x = Math.max(0, Math.min(window.innerWidth - activeWindow.offsetWidth, x));
                y = Math.max(0, Math.min(window.innerHeight - activeWindow.offsetHeight - 48, y));
                activeWindow.style.left = `${x}px`;
                activeWindow.style.top = `${y}px`;
            }
            
            function onMouseUp() {
                document.removeEventListener('mousemove', onMouseMove);
                document.removeEventListener('mouseup', onMouseUp);
                activeWindow = null;
            }
            
            document.addEventListener('mousemove', onMouseMove);
            document.addEventListener('mouseup', onMouseUp);
        });
    </script>
</body>
</html>
